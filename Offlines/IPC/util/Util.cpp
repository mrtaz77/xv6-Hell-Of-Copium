#include "Util.hpp"

unsigned int N, M, w, x, y, z;

void input_params() {
    cin >> N >> M;
    cin >> w >> x >> y >> z;
}

void output_params() {
    cout << N << " " << M << endl;
    cout << w << " " << x << " " << y << " " << z << endl;
}
